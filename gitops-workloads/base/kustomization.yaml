apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: workloads

resources:
  - deployment.yaml
  - service.yaml

commonLabels:
  managed-by: argocd
  experiment: baseline-reconciliation
